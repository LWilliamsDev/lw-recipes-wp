	<footer class="px-4 pb-8 pt-8 md:grid md:grid-cols-[1fr_1fr] md:px-12 md:pt-8">
		<div>
			<a href="/"><img src="{{ function('get_stylesheet_directory_uri') }}/assets/img/recipes-logo.svg" alt="{{__('Fit & Flavor Logo', 'lw-recipes')}}" class="w-1/2 sm:w-1/3" /></a>
			{{ content }}
		</div>
		<div class="md:justify-self-end">
				<ul class="nav md:flex md:gap-x-24">
					{% for item in menu.items %}
						<li><a href="{{item.link}}" {{ item.is_target_blank ? 'target="_blank"' }}>{{ item.title }}</a></li>
					{% endfor %}
				</ul>	
		</div>
	</footer>